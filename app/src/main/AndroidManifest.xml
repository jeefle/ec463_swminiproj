<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jeff.ec463swmp1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.example.jeff.ec463swmp1.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter></activity>

        <activity
            android:name="com.example.jeff.ec463swmp1.DataSelectActvity"></activity>

        <activity
            android:name="com.example.jeff.ec463swmp1.PlotTempActivity"></activity>

        <activity
            android:name="com.example.jeff.ec463swmp1.PlotHumActivity"></activity>

        <activity
            android:name="com.example.jeff.ec463swmp1.RoomSelectionActivity"></activity>

    </application>

</manifest>